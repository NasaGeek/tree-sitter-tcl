================================================================================
Errors that should actually work
:error
================================================================================
# Valid (but I wouldn't say idiomatic) Tcl that we can't parse.
expr 1 + 1
expr 1 {+} 1
expr {1+1}
set(a)(b) 1
# Tcl treats the last ) as a literal char
puts $arr())
# Tcl treats the first ) as a literal char
set arr()) 0
# Tcl treats $ at the end as a lteral char
set $arr($)
set arr($) 0
# Empty array name is valid
set (a) 0
puts $(a)
# Broke for the sake of nested array acceses
set $arr(()
set arr(() 0

--------------------------------------------------------------------------------
===
Errors that are really errors
:error
===
set a([) 1
# This is the only whitespace we correctly error on
set a(a ) 1
# Semicolon still ends the statement
set a(;) 1
---
